{
  "name": "@duckhunt/runner",
  "version": "0.2.0",
  "private": true,
  "description": "daemon local de agent runs (contrato 42): reclama runs de duckhunt y lanza claude code headless en la máquina del usuario con su repo, su aws cli y el mcp de duckhunt. cero deps de runtime (node 22).",
  "type": "module",
  "bin": {
    "duckhunt-runner": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@platform/config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
